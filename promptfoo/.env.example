# Promptfoo real-provider CI (H3) — optional.
# Offline safe-narrative gate always runs without keys.
# Waiting on keys for real-provider job only — never invent or commit values.

# PROMPTFOO_PROVIDER=openai
# OPENAI_API_KEY=
# GEMINI_API_KEY=
