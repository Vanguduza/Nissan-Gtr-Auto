# iOS QRScanner (stub)

Contract: `bridges/contracts/qr-inventory.ts` → `QrScannerBridge`.

**Status:** Stub only (Batch 5 prioritized Android CameraX). Implement with
**AVFoundation** — never HTML5 / WebView QR.

Canonical payload: `gtr://part/{OEM}?batch={BATCH}&valuation={FIFO|AVG}`.
