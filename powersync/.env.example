# PowerSync / sync connector — names only. Never commit real values.
# Copy to local secret stores / CI / mobile keystores as needed.

POWERSYNC_URL=
POWERSYNC_PUBLIC_KEY=
# Optional project id from PowerSync dashboard (name may vary by tenant)
POWERSYNC_PROJECT_ID=

# Supabase (already used by apps) — PowerSync connector typically uses these server-side
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=   # connector only; never ship to client apps
