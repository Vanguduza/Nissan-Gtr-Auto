# packages/shared

Shared cart/pricing math, core-charge splitting, ledger helpers, multi-currency utilities.

Consumed by web + mobile apps (`apps/ios`, `apps/android-customer`, `apps/android-management`). Do not duplicate business rules inside individual apps — see `apps/SHARED_CLIENT.md`.
