package co.zw.nissangtr.management.fleet

/**
 * Company fleet CRUD — [co.zw.nissangtr.management.rpc.RpcClient]
 * list / upsert / set status. No GPS producer dependency.
 */
object FleetModule {
    const val id: String = "fleet"
}
