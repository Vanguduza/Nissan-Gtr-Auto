/**
 * Staff live-chat feature module.
 * Inbox filters + claim / reply / close via [co.zw.nissangtr.management.rpc.RpcClient]
 * (same tables/RPCs as web `/staff/chat`).
 */
object ChatModule {
    const val id: String = "chat"
}
