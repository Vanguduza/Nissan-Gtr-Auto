package co.zw.nissangtr.management.hr

/** HR feature module — attendance clock + onboarding wizard (no payroll tax UI).
 *  Biometric profile photo: `CameraxBiometricPhotoBridge` via `:biometric-photo`.
 */
object HrModule {
    const val id: String = "hr"
}
